name: chronosphere-observability-quickstart
description: Observability as code example app with OTel auto instrumentation
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-typescript